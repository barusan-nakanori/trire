
<%= render "users/sidebar" , book:@book %>

<h1>Users#index</h1>
<% @users.each do |user| %>
	<p>Image</p>
	

	<p>Name</p>
	<span><%= user.name %></span>
	<%= link_to "Show",user_path(user.id) %>
<% end %>

